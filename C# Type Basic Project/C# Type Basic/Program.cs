/*A type establishes the blueprint for a value, dictating its 
meaning and the range of possible values a variable can hold. */
// type itu blueprint value / rancangan value

/*Predefined types, also known as built-in types, are types 
that receive special support directly from the C# compiler (int, bool, string).*/

int i = 10;
string tempat = "sekolah";


